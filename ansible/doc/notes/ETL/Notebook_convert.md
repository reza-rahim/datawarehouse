```
jupyter nbconvert --to script ETL_bronze.ipynb --output-dir=/var/fuse/spark-app

jupyter nbconvert --to script ETL_silver.ipynb --output-dir=/var/fuse/spark-app

jupyter nbconvert --to script ETL_gold.ipynb --output-dir=/var/fuse/spark-app
```
